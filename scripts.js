function nav(page) {
  switch(page){
    case 'home':
      return 
    case 'news':
      return 
    case 'updates':
      return
    case 'faq':
      return 
    case 'contact':
      return 
  }
}